import { metadata, site } from "./config"
import { footer, main } from "./menu"
import { socials } from "./social"

export const config = {
  site,
  metadata,
  mainMenu: main,
  footerMenu: footer,
  socials
}
